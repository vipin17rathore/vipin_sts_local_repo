package com.example.demo;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ApplicationContext;

import com.example.demo.repository.PersonJpaRepositoryImpl;

@SpringBootApplication
public class SpringJpaApplication /*implements CommandLineRunner*/ {

	/*
	 * @Autowired PersonJpaRepositoryImpl jpaImpl;
	 */	
	private static Logger logger=LoggerFactory.getLogger(this.class);
	
	public static void main(String[] args) {
		
		ApplicationContext applicationContext=SpringApplication.run(SpringJpaApplication.class, args);
		PersonJpaRepositoryImpl personJpaRepo=applicationContext.getBean(PersonJpaRepositoryImpl.class);
		logger.info("-------------------------------------------{}");
		
		
	}

/*	@Override
	public void run(String... args) throws Exception {
		logger.info("jpaImpl{}",jpaImpl);
		if(null !=jpaImpl)
			logger.info("allPerson data -> {}",jpaImpl.allPerson());
		
*/		
	

}
